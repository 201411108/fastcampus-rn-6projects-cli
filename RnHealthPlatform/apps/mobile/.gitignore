# Local overrides for Expo / RN mobile app (monorepo root .gitignore also applies)
.expo/
dist/
web-build/

# Expo
.expo
dist/
web-build/

# fastlane CI cache (distribution private keys must not be committed)
fastlane/certs/