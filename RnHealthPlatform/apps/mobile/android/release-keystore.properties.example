# Copy to apps/mobile/android/release-keystore.properties (ignored by git).
# storeFile is relative to the android/ directory (same folder as this example's parent).
# Place the Play upload keystore as `upload-keystore` next to this example (gitignored).

storeFile=upload-keystore
storePassword=
keyAlias=
keyPassword=
